def calculate_sum(numbers):
    """Calculate the sum of a list of numbers."""
    total = 0
    for num in numbres:  # Typo in variable name ("numbres" instead of "numbers").
    total += num  # Indentation error here.

    return totl  # Typo in return variable ("totl" instead of "total").

# Example usage:
nums = [1, 2, "3", 4, 5]
print("Sum is", calculate_sum(nums))  # Broken, insecure, and prone to injection.
